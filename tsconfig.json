{
  "//": "Root config is a thin solution shell — each app/package owns its own tsconfig. Run type-checks via `turbo run check`.",
  "extends": "./packages/tsconfig/base.json",
  "files": [],
  "exclude": ["node_modules", "**/dist"]
}
