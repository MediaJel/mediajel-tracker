# Plasmo's build output and caches. The zip CI stores as an artifact lands in dist/ too.
dist/
.plasmo/
build/
keys.json

# Local build configuration. .env.example is the documented shape; these carry real values.
.env
.env.development
.env.production

# The Playwright harness (e2e/): everything it downloads or generates, and the operator's
# credentials for the terrabis.co panel run. The reference screenshots ARE committed.
e2e/out/
e2e/fixtures/vendor/
.env.e2e

# Generated by `bun run css` from src/ui/globals.css.
src/ui/globals.built.css
